- require_js "auction"

#listing
  - @auctions.each do |type, auctions|
    %div{:id => "list_#{type}", :class => "block"}
      %h1== #{type} Auctions
      =partial :auction, :with => auctions
      
      
#sidebar.block
  #filter.inset
    #types.checklist 
      %p Auction Types
      %ul
        - @auctions.each_key do |type|
          %li
            = check_box :name => type, :checked => true, :label => type, :id => "types_#{type}"
    #statuses.checklist 
      %p Auction Status
      %ul
        %li= check_box :name => "open", :checked => true, :label => "Open", :id => "statuses_open"
        %li= check_box :name => "closed", :checked => true, :label => "Closed", :id => "statuses_closed"
    .status
      Bidding is currently OPEN.

  #bids.inset
    To view or edit an auction, click on its name in the listing to the left.